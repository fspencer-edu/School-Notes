# Introduction

# Filter Rows with `filter()`

# Arrange Row with `arrange()`

# Selecting Columns with `select()`

# Add New Variables with `mutate()`

# Grouping Summaries with `summarize()`

# Grouped Mutates (and Filters)